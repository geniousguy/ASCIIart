package modules.creator.image_creator.image_loader

/**
 * A class that loads png images and converts them to the Image class
 */
class PngImageLoader(filePath : String) extends ImageLoader(filePath : String) {

}
